---
import BaseLayout from "@/layouts/BaseLayout.astro";
import FlatCard from "@/components/ui/FlatCard.astro";
import { publications } from "@/data/cards";
---

<BaseLayout title="Publications">
  <div class="mx-auto max-w-4xl px-6 py-12">
    <header class="mb-8">
      <h1 class="serif italic text-ink" style="font-size: 28px;">Publications</h1>
      <p class="mt-2 text-ink-3">Research papers and technical writing.</p>
      <div class="mt-4 border-t-2 border-ink"></div>
    </header>
    <div class="flex flex-col">
      {
        publications.map((card) => (
          <div class="border-b border-rule">
            <FlatCard
              title={card.title}
              subtitle={card.subtitle}
              url={card.url}
              image={card.image}
              tags={card.tags}
            />
          </div>
        ))
      }
    </div>
  </div>
</BaseLayout>
