---
import { profile } from "@/data/profile";
---

<section class="py-12 animate-reveal d3">
  <h2 class="space-heading mb-6 text-lg tracking-widest text-white/90">
    News
  </h2>
  <ul class="space-y-3">
    {
      profile.news.map((item) => (
        <li class="flex gap-4 text-sm">
          <span class="mono w-20 shrink-0 text-white/40">{item.date}</span>
          <span class="text-white/70">{item.text}</span>
        </li>
      ))
    }
  </ul>
</section>
